# Prototipos de Design

Estos tres archivos HTML son los prototipos originales del equipo de Design.
Usan un runtime propio (x-dc, support.js) que no es React estándar — son
solo referencia visual y de lógica (buildSteps, flagNode, tramo).

**Nunca se importan desde el código de la app** (ver plan-meta-as-diseno.md,
sección 1).
